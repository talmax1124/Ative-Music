# Allow installing packages despite engine warnings
engine-strict=false

# Disable warnings for deprecated packages during install
audit=false

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=true